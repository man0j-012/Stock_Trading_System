# Multi-Threaded Stock Trading System (C++20)

A maintainable, extensible demo of a concurrent stock matching engine using C++20, built with OOP, a factory for order creation, and clean matching policy. 

## Build
```
mkdir build && cd build
cmake ..
cmake --build . -j
./mt_stock
```
